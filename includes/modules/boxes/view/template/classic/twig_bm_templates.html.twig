{# Copyright (c) 2013 FoxP2; http://www.oscommerce.fr #}
{# file: includes/modules/boxes/view/template/classic/twig_bm_templates.html.twig #}
<li class="nav-header">{{ constant('MODULE_BOXES_TEMPLATES_BOX_TITLE') }}</li>
<li>
<div id="box-templates-classic">
{{ data | raw }}
<p class="text-center">
<a class="btn btn-info btn-small" href="http://validator.w3.org/check?uri=referer" target="_blank"><i class="icon-fa-html5"></i>  Valid HTHL5</a>
</p>
<script>
$('.tp').click(function() { window.location = getQuery(window.location.href, "template", $(this).attr("id"))})
function getQuery (uri, key, value) {
 var re = new RegExp("([?|&])" + key + "=.*?(&|#|$)", "i");
   if (uri.match(re)) {
    return uri.replace(re, '$1' + key + "=" + value + '$2');
  } else {
    var hash =  '';
    var separator = uri.indexOf('?') !== -1 ? "&" : "?";
    if( uri.indexOf('#') !== -1 ){
      hash = uri.replace(/.*#/, '#');
      uri = uri.replace(/#.*/, '');
    }
  return uri + separator + key + "=" + value + hash;
  }
}
</script>
</div>
</li>